<div id="signin_modal" class="modal fade" role="dialog">
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal">&times;</button>
        <h4 class="modal-title">Login Form</h4>
      </div>
      <div class="modal-body">
        <div class="row">
          <div class="col-xs-6">
            <div class="well">
              <%= form_for( @user, url: session_path(@user)) do |f| %>
                <form id="loginForm" method="POST" action="/login/" novalidate="novalidate">
                  <div class="form-group">
                    <%= f.label :email, class: "control-label" %>
                    <%= f.email_field :email, class: "form-control" %>
                  </div>
                  <div class="form-group">
                    <%= f.label :password, class: "control-label" %>
                    <%= f.password_field :password, class: "form-control" %>
                  </div>
                  <div id="loginErrorMsg" class="alert alert-error hide">Wrong email og password</div>
                  <div class="checkbox">
                    <label>
                      <%= f.check_box :remember_me %> Remember login
                    </label>
                  </div>
                  <%= f.submit "Log in", class: "btn btn-success btn-block btn-signin" %>
                </form>
              <% end %>
            </div>
          </div>
          <div class="col-xs-6 another_login">
            <p class="lead">Another <span class="text-success">LOGIN</span></p>
            <ul class="list-unstyled" style="line-height: 2">
              <li>
                <a class="btn btn-block btn-social btn-twitter">
                <i class="fa fa-twitter"></i> Sign in with Twitter
                </a>
              </li>
              <li>
                <a class="btn btn-block btn-social btn-facebook">
                <i class="fa fa-facebook"></i> Sign in with Facebook
                </a>
              </li>
              <li>
                <a class="btn btn-block btn-social btn-google-plus">
                <i class="fa fa-google-plus"></i> Sign in with Google
                </a>
              </li>
              <li>
                <a class="btn btn-block btn-social btn-linkedin">
                <i class="fa fa-linkedin"></i> Sign in with Linkedin
                </a>
              </li>
            </ul>
            <p><a href="#" data-toggle="modal" data-target="#signup_modal"
                class="btn btn-info btn-block" onclick="switch_to_signup()">Yes please, register now!</a></p>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
